spindra
========

Docker container for CTFs

```
./spindra_run -h
    -o              Optional file to use [Default is opt]
    -v              OpenVPN config to use (HTB)
    -b              Launch BurpSuite
```
